def is_passing(grade):
    """
    Determines if a numeric grade is passing (60 or higher).

    Args:
        grade (int): The numeric grade to check.

    Returns:
        bool: True if the grade is passing, False otherwise.
    """
    return grade >= 60